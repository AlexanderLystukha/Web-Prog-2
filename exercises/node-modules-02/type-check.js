/**
 *
 * Adds two numbers together.
 * @param {number} a
 * @param {number} b
 * @returns {number} The sum of a + b
 * */

function add(a, b) {
    return a + b
}

const sum = add(6, "9")

add() //now shows what arguments the method needs!
console.log(sum)